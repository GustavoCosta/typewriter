/**
 * Typewriter configuration class
 */
export default class TypewriterOptions {
  /**
   * If is set to true, it will restart when it arrive at the end of commands
   */
  public loop: boolean = false;

  /**
   * The speed of typing
   */
  public typeSpeed: number = 50;
}
